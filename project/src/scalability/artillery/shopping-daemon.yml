config:
  # Replace (if needed) IP address of VM [node]
  target: http://scapp_shopping-daemon
  phases:
    - duration: 120
      arrivalRate: 500
scenarios:
  - flow:
    - get:
        url: /shopping-kart/bob
